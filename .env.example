# Frontend. Leave empty in local dev — Vite proxies /api to the Worker.
# In production, set this to your Worker origin if the UI is hosted separately.
VITE_API_URL=

# Public origin of the Mini App. Used for invite links and QR payloads.
VITE_APP_URL=http://localhost:5173
